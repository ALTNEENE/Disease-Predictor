{
  "name": "disease-prediction-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "node scripts/build-frontend.js",
    "dev": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run dev",
    "lint": "npm --prefix backend run lint"
  }
}
